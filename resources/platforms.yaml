defaults:
  platform: docker
  os: ubuntu2204
  stability: stable
  sudo: sudo
supported_platforms:
  docker:
    ubuntu2204:
      friendlyName: Ubuntu 16.04
      language:
        - java
        - python
        - shell
      toolchain:
        - env
        - jdk
        - python
restrictions: {
  }
